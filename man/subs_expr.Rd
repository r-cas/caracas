% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subs.R
\name{subs_expr}
\alias{subs_expr}
\title{Substitute symbols (expression interface)}
\usage{
subs_expr(sym, old, new)
}
\arguments{
\item{sym}{A \code{caracas_symbol} expression.}

\item{old}{Symbols to replace (character or \code{caracas_symbol}).}

\item{new}{Replacement value(s) corresponding to \code{old}.}
}
\value{
A \code{caracas_symbol} object with substitutions applied.
}
\description{
Convenience wrapper for symbolic substitution in \code{caracas_symbol} objects.
}
\examples{
if (has_sympy()) {
  x <- symbol("x"); y <- symbol("y")
  e <- (x + 1)^2 + y

  # Named arguments:
  subs_expr(e, x = 2)

  # Named list:
  subs_expr(e, list(x = 2, y = 10))

  # Works with matrices too:
  A <- matrix_sym(2, 2, "a")
  B <- matrix_sym(2, 2, "b")
  subs_expr(A \%*\% A, A = B)
}

}
\concept{caracas_symbol}
